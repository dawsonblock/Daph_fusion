"""Experimental merge methods — not in the production path.

These modules are frozen (no active development) until the production
Task Arithmetic path (TA-0 through TA-3) is exhausted.

Modules:
    agx/            — AGX v2 tournament search
    regmean/        — RegMean and RegMean++
    kfac/           — K-FAC structured merge
    surgery/        — Representation surgery
    trust_region/   — Trust-region constrained merge
    subspace/       — TSV/subspace merge
    coefficient_opt/— Differentiable coefficient optimization

These are kept for reproducibility and future research, but are NOT
part of the canonical merge pipeline.
"""
